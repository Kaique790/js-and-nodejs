const [a, b, c, d] = [3, 4, 5, 16]

const soma = a + b + c + d
const subtracao = d - b
const multiplicao = a * b
const divisao = d / a
const modulo = a % 2
const elevacao = a**b

console.log(soma, subtracao, multiplicao, -divisao, modulo, elevacao)