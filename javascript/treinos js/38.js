function div(dividendo, divisor){
    divisao = dividendo / divisor
    resto = dividendo % divisor

    console.log(`Divisão: ${dividendo} / ${divisor} = ${divisao}`)
    console.log(`Resto: ${dividendo} % ${divisor} = ${resto}`)
}

div(5, 9)